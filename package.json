{
  "name": "avli",
  "private": true,
  "engines": {
    "node": "^26.0.0",
    "pnpm": "^11.20.0"
  },
  "packageManager": "pnpm@11.20.0",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "clean": "git clean -xdf node_modules",
    "dev": "bun run --filter web dev",
    "lint": "bun run --filter web lint",
    "test": "bun run --filter web test",
    "test:ui": "bun run --filter web test:ui"
  }
}
